{
	# f0_deception console: static UI + API proxy.
	# CONSOLE_DOMAIN / API_ORIGIN are injected via Caddy's {$VAR} env syntax.
}

{$CONSOLE_DOMAIN} {
	encode gzip

	handle /api/* {
		reverse_proxy {$API_ORIGIN}
	}

	handle {
		root * /srv/www
		try_files {path} /index.html
		file_server
	}
}
